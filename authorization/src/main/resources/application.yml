spring:
  application:
    name: authorization

server:
  port: 8060

grpc:
  client:
    systemGrpcServer:
      host: 127.0.0.1
      port: 8010
      enableKeepAlive: true
      keepAliveWithoutCalls: true